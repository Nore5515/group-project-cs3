
import java.awt.Graphics;

import javax.swing.*;

public class GUI {
	
	JFrame f;
	JPanel p;
	int gridSize;
	
	public GUI(int _gridSize){
		gridSize = _gridSize;
		f = new JFrame();
		f.setVisible(true);
		f.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
		f.setSize(500, 500);
		System.out.println("Work?");
		p = new JPanel(){
			//Below is Panel Painting
			public void paintComponent(Graphics g){
				for (int x = 0; x < gridSize; x++){
					for (int y = 0; y < gridSize; y++){
						g.drawLine(0,y*(500/(gridSize-1)),500,y*(500/(gridSize-1)));
					}
					g.drawLine(x*(500/(gridSize-1)), 0, x*(500/(gridSize-1)), 500);
				}
				System.out.println("Drew Lines (panel edition)!");
			}
			//Above is Panel Painting
		};
		f.add(p);
	}
	
	public void drawGrid() {
		// TODO Auto-generated method stub
		
		
	}
	
	
	
	
	
	
}
